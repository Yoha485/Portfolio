<button>
  <slot />
</button>

<style>
  button:after {
    display: block;
    content: '';

    width: 100%;
    height: 1px;
    background: linear-gradient(90.02deg, #06fdd8 1.06%, #0e9aff 102.58%);
    transform: scaleX(0);
    transition: transform 200ms ease-out;
  }
  button:hover:after {
    transform: scaleX(1);
  }
</style>
