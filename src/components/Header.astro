---
import Button from './Button.astro';

const items = [
  {
    name: 'Main',
    href: '/#intro'
  },
  {
    name: 'Projects',
    href: '/#projects'
  },
  {
    name: 'Contacts',
    href: '#footer'
  }
];
---

<header id="header">
  <nav>
    {
      items.map((item) => (
        <Button>
          <a href={item.href}>{item.name}</a>
        </Button>
      ))
    }
  </nav>
</header>

<style>
  nav {
    height: 160px;

    display: flex;
    justify-content: flex-end;
    align-items: center;
    column-gap: 60px;

    font-weight: 500;
    font-size: 22px;
    line-height: 27px;
  }
</style>
